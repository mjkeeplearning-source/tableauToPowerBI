table returns

	column order_id
		dataType: string
		sourceColumn: order_id
	column returned
		dataType: string
		sourceColumn: returned
	partition returns = m
		mode: directQuery
		source =
			let
			    Source = PostgreSQL.Database("database-1.czag60868v0k.ap-south-1.rds.amazonaws.com", "postgres"),
			    Navigation = Source{[Schema="superstore", Item="returns"]}[Data]
			in
			    Navigation
